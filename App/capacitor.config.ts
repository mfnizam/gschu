import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mfnizam.haloam',
  appName: 'Halo AM',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
